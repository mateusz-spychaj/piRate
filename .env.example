# GitHub API Token (optional, increases rate limit)
GITHUB_TOKEN=

# OpenRouter API Key (optional, falls back to mock scores if not provided)
OPENROUTER_API_KEY=

# LLM Model to use via OpenRouter (default: openai/gpt-4o-mini)
LLM_MODEL=openai/gpt-4o-mini
